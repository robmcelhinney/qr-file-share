{
  "name": "httpserver",
  "version": "0.1.0",
  "private": true,
  "description": "simple httpserver",
  "main": "server.js",
  "bin": "bin.js",
  "dependencies": {
    "@material-ui/core": "^4.9.12",
    "archiver": "^4.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-fileupload": "^1.1.7-alpha.3",
    "meow": "^6.1.1",
    "qrcode-terminal": "^0.12.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified.\"",
    "start-server": "node server.js",
    "start": "npm run build-client && node server",
    "start-local": "npm run build-client && node server",
    "build-client": "cd client && npm run build && cd .."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "pkg": {
    "assets": [
      "client/build/**"
    ]
  }
}
